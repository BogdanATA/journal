name: Build

on:
  push:
    branches: [master, main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

      - name: Add macOS Intel target
        if: matrix.os == 'macos-latest'
        run: rustup target add x86_64-apple-darwin

      - name: Build (macOS universal)
        if: matrix.os == 'macos-latest'
        run: npm run tauri build -- --target universal-apple-darwin

      - name: Build (Windows)
        if: matrix.os == 'windows-latest'
        run: npm run tauri build

      - name: Stage artifacts and checksums (macOS)
        if: matrix.os == 'macos-latest'
        run: |
          mkdir -p release-artifacts
          cp src-tauri/target/universal-apple-darwin/release/bundle/dmg/*.dmg release-artifacts/
          cp -R src-tauri/target/universal-apple-darwin/release/bundle/macos/*.app release-artifacts/
          cd release-artifacts
          shasum -a 256 *.dmg > SHA256SUMS.txt

      - name: Stage artifacts and checksums (Windows)
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path release-artifacts | Out-Null
          Copy-Item src-tauri/target/release/bundle/msi/*.msi release-artifacts/
          Copy-Item src-tauri/target/release/bundle/nsis/*.exe release-artifacts/
          $hashes = Get-FileHash -Algorithm SHA256 release-artifacts/*.msi, release-artifacts/*.exe
          $lines = $hashes | ForEach-Object { "$($_.Hash)  $(Split-Path -Leaf $_.Path)" }
          Set-Content -Path release-artifacts/SHA256SUMS.txt -Value $lines

      - name: Upload macOS artifacts
        if: matrix.os == 'macos-latest'
        uses: actions/upload-artifact@v4
        with:
          name: journal-macos
          path: release-artifacts/*
          if-no-files-found: error

      - name: Upload Windows artifacts
        if: matrix.os == 'windows-latest'
        uses: actions/upload-artifact@v4
        with:
          name: journal-windows
          path: release-artifacts/*
          if-no-files-found: error
